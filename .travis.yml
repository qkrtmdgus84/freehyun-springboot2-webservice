language: java
jdk:
  - openjdk8

branches:
  only:
    - master

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'


script: "./gradlew clean build"

# CI 실행 완료시 메일로 알람
notifications: #Travis CI 실행 완료 시 자동으로 알람이 가도록 설정
  email:
   recipients:
      - shounghyun@gmail.com